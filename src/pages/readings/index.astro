---
import BaseLayout from '../../layouts/BaseLayout.astro';
import PageHero from '../../components/PageHero.astro';
import { services } from '../../data/services';
const serviceImages: Record<string, string> = {
  'new-client-natal-chart-reading-90': 'natal-reading',
  'new-client-natal-chart-reading-60': 'natal-reading',
  'more-clarity-within-three-months': 'more-clarity',
  'more-clarity-established-client': 'more-clarity',
  'life-changes-consultation': 'life-changes',
  'solar-return-reading': 'solar-return',
  'monthly-personal-transits': 'monthly-transits',
  'relationship-astrology-consultation': 'relationship-reading',
  'quick-check-in-one-topic': 'quick-check-in',
};
---

<BaseLayout
  title="Astrology Readings"
  description="Compare Mo Lumen Astrology's currently verified consultation types and find the one that fits your situation."
>
  <PageHero
    eyebrow="Readings"
    title="Begin with your question, not the terminology."
    lede="Each consultation offers a different angle. Compare the purpose, eligibility, duration, and price before choosing."
  />
  <section class="section">
    <div class="container">
      <div class="service-list">
        {
          services.map((service) => (
            <article class="service-row">
              <>
                <img class="service-thumb" src={`/images/${serviceImages[service.slug]}.webp`} alt="" width="420" height="300" loading="lazy" decoding="async" />
                <div>
                  <>
                    <p class="eyebrow">{service.method}</p>
                    <h2>
                      <a href={`/readings/${service.slug}/`}>{service.shortName}</a>
                    </h2>
                    <p>{service.purpose}</p>
                  </>
                </div>
                <dl>
                  <>
                    <div>
                      <>
                        <dt>Duration</dt>
                        <dd>{service.duration}</dd>
                      </>
                    </div>
                    <div>
                      <>
                        <dt>Price</dt>
                        <dd>{service.price}</dd>
                      </>
                    </div>
                    <div>
                      <>
                        <dt>Birth information</dt>
                        <dd>{service.birthInfo}</dd>
                      </>
                    </div>
                    <div>
                      <dt>Who can book</dt>
                      <dd>{service.eligibility}</dd>
                    </div>
                  </>
                </dl>
                <a class="button secondary" href={`/readings/${service.slug}/`}>
                  Full details
                </a>
              </>
            </article>
          ))
        }
      </div>
    </div>
  </section>
  <section class="section surface">
    <div class="container grid grid-2">
      <div>
        <p class="eyebrow">Need a hand choosing?</p><h2>Let your situation lead the way.</h2><p>
          The Reading Finder asks a few simple questions and explains why a verified service may
          fit.
        </p>
      </div><div class="button-row">
        <a class="button" href="/reading-finder/">Find My Reading</a><a
          class="button secondary"
          href="/how-readings-work/">How readings work</a
        >
      </div>
    </div>
  </section>
</BaseLayout>
<style>
  .service-list {
    display: grid;
    gap: 1px;
    margin-top: var(--space-6);
    background: var(--stone);
  }
  .service-row {
    display: grid;
    grid-template-columns: 11rem 1.15fr 1fr auto;
    align-items: center;
    gap: var(--space-5);
    padding: clamp(1.5rem, 4vw, 3rem);
    background: var(--paper-light);
  }
  .service-thumb { width: 11rem; height: 10rem; border-radius: 1rem 45% 1rem 1rem; object-fit: cover; }
  .service-row h2 {
    font-size: var(--step-2);
  }
  .service-row h2 a {
    text-decoration: none;
  }
  .service-row dl {
    margin: 0;
  }
  .service-row dl > div {
    display: grid;
    grid-template-columns: 7rem 1fr;
    padding: 0.45rem 0;
    border-bottom: 1px solid var(--stone);
  }
  dt {
    font-weight: 700;
  }
  dd {
    margin: 0;
    color: var(--brown);
  }
  @media (max-width: 850px) {
    .service-row {
      grid-template-columns: 1fr;
    }
    .service-row dl > div {
      grid-template-columns: 1fr;
    }
    .service-row .button {
      justify-self: start;
    }
    .service-thumb { width: 100%; height: 13rem; border-radius: 1rem 1rem 45% 1rem; }
  }
</style>
